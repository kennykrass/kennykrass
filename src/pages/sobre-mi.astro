---
import Layout from '../layouts/Layout.astro';
import '../styles/tailwind.css';

const title = 'Sobre Mí | Kennykrass';
---

<Layout title={title}>
  <section class="min-h-[70vh] flex items-center justify-center px-4">
    <div class="max-w-3xl text-center">
      <h1 class="text-4xl md:text-6xl font-extrabold mb-8 leading-tight">
        <span class="bg-gradient-to-r from-blue-600 to-purple-600 dark:from-accent dark:to-blue-400 bg-clip-text text-transparent">
          Sobre Mí
        </span>
      </h1>

      <blockquote class="text-2xl md:text-3xl text-gray-900 dark:text-text-primary leading-relaxed font-semibold">
        <p>No soy promedio.</p>
        <p>Soy el resultado de la determinación,</p>
        <p>de la claridad,</p>
        <p>y de un pacto inquebrantable conmigo mismo:</p>
        <p class="mt-6 text-3xl md:text-4xl bg-gradient-to-r from-blue-600 to-purple-600 dark:from-accent dark:to-blue-400 bg-clip-text text-transparent">Nunca fallarme.</p>
      </blockquote>

      <div class="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4">
        <a href="/" class="border-2 border-blue-600 dark:border-accent text-blue-600 dark:text-accent font-semibold py-2 px-6 rounded-lg hover:bg-blue-600 dark:hover:bg-accent hover:text-white dark:hover:text-background transition">
          Ir al inicio
        </a>
        <a href="/#contacto" class="text-blue-600 dark:text-accent hover:underline font-medium">Contactar →</a>
      </div>
    </div>
  </section>
</Layout>

